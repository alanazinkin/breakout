/*package breakout;

import java.util.ArrayList;
import javafx.scene.Group;
import javafx.scene.Scene;

public class Level {
    ArrayList<Block> blocks;
    int myLevel;

    Level(int myLevel, ArrayList<Block> blocks) {
        this.myLevel = myLevel;
        this.blocks = blocks;
    }

    // initLevels(int numLevels, )

    public void endLevel(Group root) {
        root.getChildren().removeAll(blocks);
    }

}

 */
